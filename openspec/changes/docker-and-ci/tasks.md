# Docker and CI/CD — Tasks

- [ ] Write multi-stage Dockerfile
- [ ] Verify `docker build` succeeds locally
- [ ] Write `.github/workflows/ci.yml`
- [ ] Confirm PR workflow skips docker push step
- [ ] Confirm push-to-main workflow includes docker push step
- [ ] Verify build: `dotnet build mock-s3.slnx` passes with 0 warnings
- [ ] Verify tests: `dotnet test` passes
